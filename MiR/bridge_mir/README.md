# bridge_mir

**File:** `mir_raw.py`

MiR200 ROS1->ROS2 bridge (roslibpy). Translates the MiR topics/services into ROS2.
This is the "MiR bridge": the MiR has no ROS package of its own in the workspace, its
integration lives here.
