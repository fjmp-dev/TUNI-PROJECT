# ROUTER_MODEM (SIM / network)

Configuration of the **Teltonika RUTX50** router that provides the network via SIM.

> **To investigate (phase 2):** analyze the router config (management IP, SIM APN, WiFi 192.168.1.x for the MiR). Access credentials stored separately. See the known MiR WiFi issue.
